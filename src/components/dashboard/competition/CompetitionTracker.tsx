import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export const CompetitionTracker = () => {
  return (
    <div className="p-6">
      <Card>
        <CardHeader>
          <CardTitle>Competition Tracker</CardTitle>
        </CardHeader>
        <CardContent>
          Competition tracking tools will go here
        </CardContent>
      </Card>
    </div>
  )
}